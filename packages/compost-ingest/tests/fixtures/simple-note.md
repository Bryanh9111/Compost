# Project Setup

This document describes how to set up the project. You need Node.js 20+ and Python 3.11+.

## Installation

Run `bun install` to install dependencies. Then run `uv sync` for Python packages.

## Configuration

Create a `.env` file with your API keys. The default configuration works for local development.
