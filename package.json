{
  "name": "compost",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test",
    "lint": "biome check .",
    "check:engram-boundary": "bun scripts/check-engram-boundary.ts",
    "bench": "bun bench/sqlite-reflect.bench.ts; bun bench/sqlite-query.bench.ts; bun bench/lancedb-ann.bench.ts; bun bench/llm-latency.bench.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/bun": "^1.2.0"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.27.2"
  },
  "overrides": {
    "hono": "4.12.14"
  }
}
